/* C program to find the sum of 50 natural numbers */
#include <stdio.h>
int main()
{
	int sum, i;
	for(i=1;i<=50; i++)
	{
		sum = sum + i;
		
	}
	printf("Sum of first 50 natural numbers is: %d", sum);
	return 0;
	}

